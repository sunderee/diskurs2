name: diskursv2
description: Remake of the Diskurs application

publish_to: "none"
version: 0.1.0+4

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Architecture
  flutter_bloc: ^7.2.0
  intl: ^0.17.0

  # Code quality
  meta: ^1.7.0
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linter
  flutter_lints: ^1.0.4

flutter:
  generate: true
  uses-material-design: true
